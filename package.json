{
  "name": "awscognito",
  "description": "AWS Cognito CLI tool for exporting and importing users",
  "version": "0.0.1",
  "main": "./src/cli.ts", 
  "scripts": {
    "build": "tsc -p .",
    "dev": "tsc -w",
    "compile": "tsc -p .",
    "lint": "tslint **/*.ts",
    "prepare": "npm run build"
  },
  "bin":{
     "awscognito" : "build/src/cli.js"
  },
  "dependencies": {
    "aws-sdk": "^2.226.1",
    "meow": "^4.0.0"
  },
  "devDependencies": {
    "@types/meow": "^4.0.1",
    "@types/node": "^7.0.60"
  }
}
