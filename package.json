{
  "name": "awscognito",
  "description": "AWS Cognito CLI tool for exporting and importing users",
  "version": "0.0.1",
  "main": "./src/cli.ts",
  "scripts": {
    "awscognito": "node build/cli.js export-users",
    "build": "tsc -p .",
    "dev": "tsc -w",
    "compile": "tsc -p .",
    "lint": "tslint **/*.ts",
    "prepare": "npm run build"
  },
  "bin": {
    "awscognito": "build/cli.js"
  },
  "dependencies": {
    "@types/lodash.omit": "^4.5.6",
    "aws-sdk": "^2.226.1",
    "lodash.omit": "^4.5.0",
    "meow": "^4.0.0"
  },
  "devDependencies": {
    "@types/meow": "^4.0.1",
    "@types/node": "^7.0.60",
    "typescript": "^3.8.3"
  }
}
